import { SET1, SET2} from './soundGroup'
const initialState = {
    SET: true,
    DATA1: SET1,
    DATA2: SET2,
    DISPLAY: 'Drum Machine',
    KEYPRESS: {},
    PLAY: null,
    POWER: true,
    volume: 50
}
export default initialState